{
  "rules": {
    ".read": true,
    ".write": false,

    "dot": {
      ".write": true,
      ".validate": "newData.hasChildren(['client', 'position'])",

      "client": {
        ".validate": "newData.isString()"
      },

      "position": {
        ".validate": "newData.hasChildren(['x', 'y'])",

        "x": {
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100"
        },

        "y": {
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100"
        }
      }
    }
  }
}
